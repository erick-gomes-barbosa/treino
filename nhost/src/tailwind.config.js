module.exports = {
    content: ['./src/*.{html, js, ts, tsx}'],
    theme: {
        extend: {
            fontFamily: {
                KG: ['Lato', 'serif'],
            }
        }
    },
    plugins: [],
}